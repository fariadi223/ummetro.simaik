[supervisord]
nodaemon=true
user=root

logfile=/dev/null
logfile_maxbytes=0

pidfile=/run/supervisord.pid

[program:php-fpm]
command=/usr/local/sbin/php-fpm --nodaemonize

priority=10

autostart=true
autorestart=true

startsecs=2
startretries=5

stopasgroup=true
killasgroup=true

stopsignal=QUIT

stdout_logfile=/dev/stdout
stdout_logfile_maxbytes=0

stderr_logfile=/dev/stderr
stderr_logfile_maxbytes=0

[program:laravel-worker]
command=/usr/local/bin/php /var/www/html/artisan queue:work --sleep=3 --tries=3 --timeout=120

directory=/var/www/html

user=www-data

priority=20

autostart=true
autorestart=true

startsecs=2
startretries=5

stopasgroup=true
killasgroup=true

stopsignal=TERM

stdout_logfile=/dev/stdout
stdout_logfile_maxbytes=0

stderr_logfile=/dev/stderr
stderr_logfile_maxbytes=0